export default async function handler(req, res) {
  // Query Supabase → conta atendimentos, serviços, fechados, problemas
  // Envia no Telegram
  // (código completo pronto – me avise se quiser agora)
}
